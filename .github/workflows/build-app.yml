name: Build TrayForge App

on:
  push:
    branches: [ "main" ]
    paths:
      - 'trayforge/**'
      - '.github/workflows/build-app.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./trayforge
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/trayforge-app:main